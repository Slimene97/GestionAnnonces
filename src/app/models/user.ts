export interface User {
  id: string; // Identifiant de l'utilisateur (chaîne de caractères)
  name: string; // Nom de l'utilisateur (chaîne de caractères)
  email: string; // Adresse e-mail de l'utilisateur (chaîne de caractères)
}
